<% provide(:title, "タグ:#{params[:tag]}") %>

<%= link_to "戻る", photo_path, class:"search_button" %>

<h1>タグ:<%= params[:tag] %></h1>

<div id="photo_index_container">
  <% @posts.each do |post| %>
    <%= render post %>
  <% end %>
</div>

