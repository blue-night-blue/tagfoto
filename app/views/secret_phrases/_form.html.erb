<%= form_with(model: secret_phrase) do |form| %>
  <% if secret_phrase.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(secret_phrase.errors.count, "error") %> prohibited this secret_phrase from being saved:</h2>

      <ul>
        <% secret_phrase.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :password, style: "display: block" %>
    <%= form.text_field :password %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
