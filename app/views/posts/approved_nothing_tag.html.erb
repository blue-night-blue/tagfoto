<% provide(:title, "#{@user.name}(タグなし)") %>

<%= link_to "戻る", approved_index_path , class:"button button_link" %>

<h1><%= @user.name %>のタグなし写真</h1>

<div id="photo_index_container">
  <% @posts.each do |post| %>
    <%= render "post_approved",post:post %>
  <% end %>
</div>

