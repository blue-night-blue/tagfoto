<%= link_to "#{@user.name}の写真一覧に戻る", "/photo/#{@user.name}", class:"search_button" %>

<% if @post.images.attached? %>

<div>
    <%= @post.tag %>
  </div>
  <div>
    <%= link_to @post.images[0] do %>
      <%= image_tag @post.images[0] %>
    <% end %>
  </div>
<% end %>

